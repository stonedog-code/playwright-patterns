# Copy to .env and fill in. Never commit .env.
BASE_URL=https://demo.example.com
API_BASE_URL=https://demo.example.com/api

# A pre-seeded account used ONLY by the login UI spec and by the API helper
# that mints tokens for every other spec.
TEST_USER_EMAIL=qa.bot@example.com
TEST_USER_PASSWORD=replace-me
